# Database Configuration
# Replace the API key with your own from Prisma Data Platform
DATABASE_URL="prisma+postgres://accelerate.prisma-data.net/?api_key=YOUR_API_KEY_HERE"

# Optional: Local PostgreSQL fallback connection (for development)
# DATABASE_URL="postgresql://username:password@localhost:5432/extension_inventory?schema=public"

# Additional configurations can be added here

